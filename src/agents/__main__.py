"""Allow running agents as modules: python -m src.agents.librarian"""
# This file exists so that `python -m src.agents` works,
# but individual agents have their own __main__ handling via main() functions.
